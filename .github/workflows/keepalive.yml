name: Keep Streamlit App Warm

on:
  schedule:
    # 12時間おきに実行（UTC 0時・12時 → JST 9時・21時）
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl Streamlit app (3min-engine)
        run: |
          curl -sS "https://victorconsulting-3min-engine-ybr9nvztte9wuhajxf4fgg.streamlit.app/" > /dev/null
